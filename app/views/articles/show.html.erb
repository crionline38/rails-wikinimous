<a href=" <%= articles_path %> ">Retour a la liste</a>
<h1> <%= @article.title %> </h1>



<p> <%= Kramdown::Document.new(@article.content).to_html.html_safe %>
  <hr>
      <%= link_to "Edit", edit_article_path(@article) %>
      <%= link_to "Delete", article_path(@article),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
</p>
